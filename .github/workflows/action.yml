on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Trying to run docker
        run: docker ps
      - name: checkout repo
        run: actions/checkout@v2.3.4
      - name: Watch file
        run: ls -la

