on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Trying to run docker
        run: docker ps
      - name: checkout repo
        run: git clone https://github.com/MomorioUHT/FrontEnd.git
      - name: Watch file
        run: ls -la

